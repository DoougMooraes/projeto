/**
 * Modelo do usuário.
 * Estrutura da tabela 'usuarios':
 *   id      INT AUTO_INCREMENT PRIMARY KEY,
 *   nome    VARCHAR(255),
 *   email   VARCHAR(255) UNIQUE,
 *   senha   VARCHAR(255)
 */
