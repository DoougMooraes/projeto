/**
 * Modelo do estabelecimento.
 * Estrutura da tabela 'estabelecimentos':
 *   id          INT AUTO_INCREMENT PRIMARY KEY,
 *   nome        VARCHAR(255),
 *   endereco    VARCHAR(255),
 *   categoria   VARCHAR(100),
 *   banner      VARCHAR(255),
 *   descricao   TEXT
 */
